<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lqzc.mapper.CustomerUserMapper">

    <!-- 基础结果映射 -->
    <resultMap id="BaseResultMap" type="com.lqzc.common.domain.CustomerUser">
        <id property="id" column="id"/>
        <result property="nickname" column="nickname"/>
        <result property="phone" column="phone"/>
        <result property="avatar" column="avatar"/>
        <result property="email" column="email"/>
        <result property="gender" column="gender"/>
        <result property="level" column="level"/>
        <result property="status" column="status"/>
        <result property="registerChannel" column="register_channel"/>
        <result property="lastLoginTime" column="last_login_time"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <!-- 基础列定义 -->
    <sql id="Base_Column_List">
        id, nickname, phone, avatar, email, gender, level, status,
        register_channel, last_login_time, create_time, update_time
    </sql>

    <!--
        查询客户订单统计信息
        统计条件：
        - 只统计已完成的订单（order_status = 3）
        - 只统计已支付的订单（pay_status = 1）
        返回字段：
        - totalOrders: 订单总数
        - totalSpent: 消费总额（使用payable_amount实际支付金额）
    -->
    <select id="selectOrderStats" resultType="java.util.Map">
        SELECT
            COUNT(1) AS totalOrders,
            COALESCE(SUM(payable_amount), 0) AS totalSpent
        FROM order_info
        WHERE customer_id = #{customerId}
          AND order_status = 3
          AND pay_status = 1
    </select>

</mapper>
